# Verde Tech lessons (student self-paced pages)
Public GitHub Pages site so students can open lessons from Google Classroom on Chromebooks — works even when Leo's Mac is off.
Add a lesson: drop its `*.html` here, link it in `index.html`, then `git add -A && git commit -m "add lesson" && git push`.
